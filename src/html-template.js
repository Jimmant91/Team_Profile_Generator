//Creating the employee team
const generateTeam = team => {

    //Manageer
    const generateManager = manager => {
        return `
        <div class="card employee-card">
        <div class="card-header">
            <h2 class="card-title">${manager.get}
        `
    }
}